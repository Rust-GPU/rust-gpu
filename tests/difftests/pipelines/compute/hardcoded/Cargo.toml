[package]
name = "hardcoded"
version = "0.0.0"
publish = false
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[workspace]

[dependencies]
spirv-std = { path = "../../../../../crates/spirv-std" }
