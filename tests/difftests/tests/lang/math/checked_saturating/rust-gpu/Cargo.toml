[package]
name = "math-checked-saturating-rust-gpu"
edition.workspace = true

[lints]
workspace = true

[dependencies]
math-checked-saturating-cpu = { path = "../cpu" }

# CPU deps (for the test harness)
[target.'cfg(not(target_arch = "spirv"))'.dependencies]
difftest.workspace = true
